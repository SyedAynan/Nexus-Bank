# Production build environment variables for Vercel
# The /api path is proxied to Render backend via vercel.json rewrites
# This means the frontend makes same-origin requests — no CORS needed
VITE_API_URL=/api
